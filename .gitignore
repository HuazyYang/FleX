# we do not store binary outputs in Flex

# windows
*.exp
*.ilk
.vscode/*
.vscode/tasks.json
.vscode/launch.json
.vscode/settings.json
.cache
build
.run
.toolchains
demo/compiler
extensions/compiler
src/dx/context

# android
*.apk

# Locally fetched FXC 6.3.9600.16384 (Win 8.1 SDK), used to reproduce the
# shipped DXBC exactly. Microsoft binaries - fetched, never committed.
/tools/fxc63/

# Reference videos matched against by the XPBD demo scenes (the paper's
# supplementary material). Large binaries - kept locally, never committed.
/data/*.mp4
